math = import("math");

print(
    "pi =", math.libpi.pi,
    "add(pi, 5) =", math.add(math.libpi.pi,5)
);

print(math.circle.area(2.5));
print(math.circle.circumference(2.5));

assert(math.libpi.pi==22/7);

# test assignment into module
math.weee = ()=>ret "Weee!";
assert(math.weee()=="Weee!");
